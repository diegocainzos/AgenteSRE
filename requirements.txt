# Minimal dependencies needed to run the example code and tests
#
# This pared‑down list covers only the packages that are explicitly imported
# by the application (src/) and by the unit tests.  All other packages found
# in the full "requirements.txt" are part of the development environment
# and can be omitted from a lightweight deployment.

python-dotenv>=1.0.0
langchain-core>=0.0.0
langchain-google-genai>=0.0.0
langchain-chroma>=0.0.0
langchain-text-splitters>=0.0.0
langchain-classic>=0.0.0
langgraph>=0.0.0
pydantic>=2.0
fastapi>=0.80
uvicorn>=0.18
requests>=2.0

# note: the "langchain-..." packages are named to match the import
# statements used in the code.  Adjust versions as required by your
environment.
